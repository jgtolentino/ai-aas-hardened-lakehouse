export interface ExecutiveKPIs {
  revenue: number;
  transactions: number;
  market_share: number;
  stores: number;
}
export interface TrendPoint { d: string; rev: number }
export interface TopBrand { name: string; v: number }
