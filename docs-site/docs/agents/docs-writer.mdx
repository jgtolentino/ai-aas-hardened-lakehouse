---
id: agent-docswriter
title: "Agent: DocsWriter"
sidebar_label: "Agent: DocsWriter"
description: "AI docs generator for Docusaurus + GitHub Wiki mirror"
tags: [agent, mcp, docs]
---

> AI docs generator for Docusaurus + GitHub Wiki mirror

## Summary
This page is autogenerated from `mcp/agents/docs-writer.yaml`.

## Setup
1. Place the spec in `mcp/agents/docswriter.yaml`.
2. Configure required `.env` keys (no secrets in repo).

## Run
```bash
pnpm docs:gen
pnpm docs:build
```

## Spec (reference)
```yaml
name: DocsWriter
role: AI docs generator for Docusaurus + GitHub Wiki mirror
entrypoint:
  type: shell
  cmd: |
    set -euo pipefail
    pnpm install
    pnpm -C docs-site install || true
    node scripts/generate-agent-docs.mjs
    pnpm docs:lint
    pnpm docs:build
    PNPMFILE=scripts/wiki-sync.sh bash scripts/wiki-sync.sh
permissions:
  filesystem:
    - docs-site/**
    - mcp/**
    - agents/**
    - scripts/**
environment:
  DOCUSAURUS_DIR: docs-site
  WIKI_REPO: https://github.com/jgtolentino/ai-aas-hardened-lakehouse.wiki.git
  WIKI_SRC: docs-site/wiki
  WIKI_BRANCH: master
routing:
  model: gpt-5
  fallback_model: gpt-4o-mini
  max_tokens: 8000
  temperature: 0.2
__src: mcp/agents/docs-writer.yaml
```

## Validate
```bash
pnpm docs:lint && pnpm docs:linkcheck
```

## Troubleshoot
- If sidebar missing, check `docs-site/sidebars.js` export.
- Clear cache: `pnpm -C docs-site docusaurus clear`.
